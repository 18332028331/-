#define  _CRT_SECURE_NO_WARNINGS 1
#include<stdio.h>
#include<string.h>
int main()
{
//利用结构体类型创建一个该类型的结构体变量
struct Book b1 = { "C语言程序设计",55 };
strcpy(b1.name, "c++");//strcpy-string copy-字符串拷贝-库函数需加头文件-string.h
printf("%s\n", b1.name);
return 0;
}
